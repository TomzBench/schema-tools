openapi: "3.1.0"
info:
  title: Alpha API
  version: "1.0.0"
paths:
  /items:
    get:
      operationId: listItems
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            format: int32
