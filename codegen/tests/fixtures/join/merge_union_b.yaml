openapi: "3.1.0"
info:
  title: Test API
  version: "1.0.0"
components:
  schemas:
    alpha:
      type: object
      required:
        - code
      properties:
        code:
          type: integer
          format: uint16
    beta:
      type: object
      properties:
        active:
          type: boolean
