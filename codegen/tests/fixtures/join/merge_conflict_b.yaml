openapi: "3.1.0"
info:
  title: Beta API
  version: "2.0.0"
paths:
  /items:
    get:
      operationId: listItems
      parameters:
        - name: limit
          in: query
          schema:
            type: string
            maxLength: 10
