components:
  schemas:
    gadget:
      type: object
      properties:
        name:
          type: string
          default:
            required:
              - zebra
              - apple
          x-meta:
            tags:
              - beta
              - alpha
      dependentRequired:
        name:
          - label
          - desc
      $defs:
        mode:
          type: string
          enum:
            - on
            - off
          examples:
            - on
            - off
